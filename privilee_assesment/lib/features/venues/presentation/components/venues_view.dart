import 'package:flutter/material.dart';

class VenuesView extends StatelessWidget {
  const VenuesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Hello Privilee'));
  }
}
